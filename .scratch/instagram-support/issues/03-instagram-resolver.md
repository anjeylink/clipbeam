# 03-instagram-resolver

Status: resolved

Spec: ../spec.md

Blocked by: 01, 02

Captured fixtures (__fixtures__/instagram), embed mapper (rich contextJSON + simple srcset), page mapper on the shared Meta JSON module (media-carrying node only), resolve-instagram-post.ts with share-link following, wired into /api/resolve via an exhaustive switch; Instagram allowlist entry.
